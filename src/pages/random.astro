---
import { getRandomXValue, getRandomYValue } from '../lib/utils';
// Get random values
const randomX = getRandomXValue();
const randomY = getRandomYValue();
// Redirect to the post page with random X and Y values
return Astro.redirect(`/post/${randomX.id}/${randomY.id}`); 
---